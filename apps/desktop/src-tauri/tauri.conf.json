{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "RunPod Launcher",
  "version": "0.1.0",
  "identifier": "com.runpodlauncher.desktop",
  "build": {
    "beforeDevCommand": "npm run dev",
    "devUrl": "http://localhost:5173",
    "beforeBuildCommand": "npm run build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "title": "RunPod Launcher",
        "width": 1000,
        "height": 720,
        "minWidth": 480,
        "minHeight": 480,
        "resizable": true
      }
    ],
    "security": {
      "csp": "default-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self' ipc: http://ipc.localhost http://* https://*"
    }
  },
  "bundle": {
    "active": true,
    "targets": ["dmg", "app", "msi", "nsis"],
    "icon": ["icons/32x32.png", "icons/128x128.png", "icons/icon.icns", "icons/icon.ico"],
    "copyright": "MIT",
    "category": "DeveloperTool",
    "shortDescription": "Run your own model on rented GPUs",
    "longDescription": "Starts and stops RunPod GPU pods that serve an OpenAI-compatible endpoint, so a self-hosted model can sleep when nobody is using it."
  }
}
