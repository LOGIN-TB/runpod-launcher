import { type ReactNode } from 'react';
/** A read-only value with a copy button — addresses, tokens, commands. */
export declare function CopyField({ value, secret }: {
    value: string;
    secret?: boolean;
}): ReactNode;
//# sourceMappingURL=CopyField.d.ts.map